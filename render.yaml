# Static site przdnt.com на Render (бесплатно).
# API форм — отдельный репозиторий s-a-tselikov/tts-mail (Web Service tts-mail).
services:
  - type: web
    name: old-sites
    runtime: static
    branch: przdnt
    buildCommand: echo "static przdnt.com"
    staticPublishPath: .
